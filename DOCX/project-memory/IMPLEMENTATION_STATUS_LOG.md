# Implementation Status Log

This is append-only. The tracker remains the live status authority.

## 2026-09-16 — Pre-Phase-12 system-wide hardening and regression lock

Established the frozen Phase 0-11 baseline before any Ticket System work. Added the fail-closed `npm run test:system-lock` orchestrator, the fresh-system `npm run test:system-smoke` runner, the concurrency/rollback `npm run test:system-concurrency` repeatability runner, the `scripts/verify-scenario-manifest.mjs` manifest validator, the `test/system-lock/scenario-manifest.json` registry (63 scenarios, 62 automated and 1 manual-only), and the human-readable `SYSTEM_WIDE_TEST_SCENARIO_CATALOG.md`. The manifest validator fails on duplicate scenario ids, missing evidence, a referenced-but-absent test file, a registered Phase 0-11 test file silently omitted from the authoritative runners, a skipped scenario without a reason, or an implemented protected route absent from route certification. The two consecutive system-lock runs both passed 14/14 with zero interruptions, the concurrency/rollback repeat passed 21/21 (three passes by seven suites), and the full maintained verification contract (typecheck, 75/75 unit, 45/45 component, 11/11 aggregate integration suites at 105 tests, 8/8 migration, 13/13 route certification, isolation 9/9, fictional seed smoke, fresh-system smoke, 11/11 aggregate E2E suites at 72 tests with zero skips, lint, isolated safe build, and diff checks) passed. No genuine Phase 0-11 settled-behaviour defect was found or repaired; no product behaviour, schema, privacy, authorization, validation, audit, notification, transaction, or concurrency guarantee changed. Phase 12 was not started. `prototype/full-frontend-r1/`, the historical remediation script, every `.env*` file, and the Preview worktree were left untouched, and no production system or `.env.production` value was accessed.

## 2026-09-16 — Post-Phase-11 dashboard acceptance and manifest reconciliation

Reconciled two possible inconsistencies in the Phase 11 completion receipt against the repository. The commit-manifest discrepancy was a conversational miscount only: Git reports 43 files for `e08f76f` as 11 documentation, 7 harness/configuration, 20 application and 5 tests, while the receipt said 10/7/21/5. No authoritative document contained a committed-file count, so no documentation was inaccurate on that point and the Phase 11 commit was not amended. The dashboard discrepancy was a genuine acceptance gap: the Super Admin dashboard delivered ten of the twelve approved surfaces (the `Schedule lifecycle` and `Recent recorded actions` tables were missing) and the Employee dashboard delivered four of the five approved areas (`My leave and balance` was missing). Both were added using the existing `schedule-lifecycle` report contract, the existing Phase 10 audit allowlist and the authoritative `leaveService.getMyLeave` computation; section surfaces now carry a question, an explicit empty state and a drill-down, and the labelled table region renders even when a surface is empty so an empty surface stays named and announced. Tests were strengthened rather than weakened: three new integration scenarios, four new component scenarios and three strengthened E2E journeys. No metric definition, authorization rule, privacy rule, export rule, conflict-fact value or R4/R10 boundary changed. Every mandatory gate passed, Phase 11 remains `COMPLETED`, and Phase 12 was not started.

## 2026-09-16 — Phase 11 completed

Implemented the bounded reporting journey from `52b01f9bfd078fa15ead12dbdd32b907f8630e60` on `main`. `/dashboard` replaced its Phase 1 shell with a role-branched operational summary and `/reports` became a scope-enforced index over thirteen registered metric contracts, each declaring its source of truth, grain, inclusion and exclusion rules, date interpretation, role policy, privacy class, ordering and empty-state behaviour. Authoritative staffing metrics read only the current Published schedule; the Draft and Proposed planning view is a separate `planning-unpublished` report labelled `PLANNING (unpublished)` in the page, heading, filename and on every exported row, available to a scoped Admin only for records inside their current effective scope and permanently closed to Employees. Availability terminology was removed: the only employee-related derived fact is a conflict fact with four permitted values, computed from active status, approved leave and current Published overlap, never from `working_pattern` and never as a staffing verdict. Exports stream bounded CSV re-authorized per request, capped at 5,000 rows with refusal rather than truncation, protected against spreadsheet formula injection, and audited with safe metadata only; a scoped Admin may export exactly the Published allocation and the certification-summary projection, planning is view-only for Admin, and audit history has no export. No migration was required — every metric reads existing columns, so the database remains at 32 tables and 12 ledger rows. Every mandatory gate passed: typecheck, 75/75 unit, 41/41 database-free component, 11/11 aggregate integration suites (102/102), the focused Phase 2–11 service runners, 8/8 migration, 11/11 aggregate E2E suites (72/72 with zero skips), 13/13 route certification, isolated safe build, fictional seed smoke, isolation 9/9, lint, and both whitespace checks. No production system, secret or `.env.production` value was accessed; `prototype/full-frontend-r1/`, the historical remediation script and the Preview worktree were left untouched. Phase 12 was not started.

## 2026-09-15 — Post-Phase-10 management-note authorization remediation

Verified and corrected a real access-control defect in the Phase 10 employee-management-note policy. The shipped commit evaluated authorship before role and scope: `canReadManagementNote` returned `true` for the note author unconditionally and `listAuthoredByActor` admitted any current role, so a demoted author who is now an Employee, an Admin whose subject scope was revoked, and an Admin moved to another team all retained access to notes they had previously written. A pre-remediation probe recorded `demotedAuthor: true, outOfScopeAuthor: true, otherTeamAuthor: true`. The approved rule now requires **both** note-level visibility permission and current authorization to manage/read the subject: the service re-reads the acting user's current role, active status, and current scope grants on every read, list, count, direct-id lookup, archive attempt, and employee-detail panel render, and authorship never overrides them. The policy-free `activeNoteCount` repository helper was removed and replaced with a policy-applied count so counts cannot leak existence. Added `test/integration/phase10-management-note-authorization.test.ts` (15 disposable-PostgreSQL scenarios that mutate roles, active flags, and TEAM grants between calls), strengthened the Phase 10 integration and unit policy suites, and registered the new file in the Phase 10 focused runner and the aggregate integration gate. Independently reconciled the Phase 10 commit manifest from Git: 68 files (36 added, 32 modified) composed of 15 documentation, 9 harness/configuration, 39 application, and 5 test files, correcting the previously reported category labels of 12/41/6. No Phase 11 work began; the prototype, the historical remediation script, `.env*`, and the Preview worktree were untouched.

## 2026-09-18 — Existing-user credential authentication remediation (R1)

Replaced the fictional-persona selection screen with username/email-and-password authentication to resolve the Production `Mock authentication is unavailable.` error. Added the additive `0012_existing_user_credential_authentication.sql` migration (`user_credentials` table plus the `password` enum value; 33 public tables, 13-migration ledger), a versioned scrypt password layer with a server-only `AUTH_PASSWORD_PEPPER`, a strict `POST /api/auth/login` credential route, database-backed five-in-fifteen-minute lockout, and the guarded one-time bootstrap (`scripts/bootstrap-existing-user-credentials.ts`). The five existing user IDs, roles, scope grants, and relationships are preserved; authentication proves identity only and authorization re-reads role, active state, grants, and `session_version` from PostgreSQL on every request. Mock login is now test-only and returns a neutral refusal in Production regardless of `MOCK_AUTH_ENABLED`. Login UI, audit events (`auth.password_session.started/refused/ended`, `auth.credentials.bootstrapped`), the Phase 10 audit allowlist, the aggregate integration and E2E runners, the route-certification cases, and the system-lock scenario manifest were updated. A new credential integration suite and a desktop/mobile credential E2E journey were added. No Phase 12 work was performed; the Preview worktree, the prototype, the historical remediation script, and `.env*` files were left untouched, and no password, pepper, connection string, token, or hash appears in source, logs, or docs.

## 2026-09-15 — Phase 10 completed

Implemented the bounded collaboration-and-governance journey from `64fd480a5d099a127d9ea62ef0e53a819f1e5d7a` on `main`. Shared Client, Project, and Location notes gained an additive `operational_note_revisions` history so every edit preserves the superseded content in the same transaction, with author-only editing and Super Admin-only reasoned archive; the product owner confirmed that these notes are limited to authenticated users already authorized on the parent record, so Employees hold no shared-note access and NTE-005 was reconciled accordingly. Employee-management notes gained a real repository, service, validation, action, authorization, audit, and employee-detail panel over the preserved schema and policy, enforcing private-to-author and shared-upward visibility with immutable content and one non-enumerating refusal for the subject, peers, and out-of-scope Admins. Additive `discussion_threads` and `discussion_messages` tables back participant-only replacement-request discussions whose participants are derived live from the request, with message, safe audit, and per-participant notification written in one transaction. `/notifications` became a complete per-recipient inbox with independent, idempotent read and archive state, stable pagination, and server-reauthorized related-record navigation, and `/audit` became a Super Admin-only read-only history rendered strictly through a per-action safe metadata allowlist. The approved Phase 9 evidence-integrity prerequisite resets review and verification provenance whenever an owner materially changes evidence, without changing Phase 7 coverage. Every mandatory gate passed: typecheck, 56/56 unit, 32/32 database-free component, 10/10 aggregate integration suites (63/63), the Phase 2–10 focused service runners, 8/8 migration, 10/10 aggregate E2E suites (66/66 with zero skips), 12/12 route certification, lint, isolated safe build, fictional seed smoke, isolation 9/9, and both whitespace checks. No production system, secret, or `.env.production` value was accessed; `prototype/full-frontend-r1/`, the historical remediation script, and the Preview worktree were left untouched. Phase 11 was not started.

## 2026-09-02 — Phase 7 implementation

Implemented the bounded coverage/replacement journey with additive request persistence, independent staffing-rule gaps, scoped candidate filtering, Super Admin-only decisions, and non-published schedule effects. Targeted migration, typecheck, and disposable PostgreSQL tests passed; no production access occurred.

## 2026-09-02 — Phase 5 closure continuation

Phase 5 leave and availability implementation resumed from local `main` at `0c00b91e0613cbd7d98c8c5af5282a20da22e2a9`, aligned with `origin/main`. The prior `BLOCKED` classification had no external blocker; remaining work was in-scope verification, documentation, focused review, and delivery. No production system was accessed.

## 2026-09-02 — Phase 5 completed

The bounded annual-leave journey passed migration, unit, PostgreSQL integration, targeted lint, typecheck, build, desktop/mobile browser, and diff checks. The Phase 5 report, tracker, context, index, decision record, Definition of Done, and closure evidence were updated. Coverage, replacement, skills, maps, tickets, payroll, attendance, production access, and deployment remain outside Phase 5.

## 2026-09-02 — Phase 6 completed

The controlled skill and operational-capability journey reused the Phase 2 catalogue and employee-skill stores, added only versioned archival assignment requirements, connected the existing Client/Project/Location requirement records, and surfaced source-attributed server-calculated Super Admin warnings in the Phase 4 review flow. Targeted migration, unit, component, disposable PostgreSQL service, desktop/mobile browser, typecheck, build, and diff evidence is recorded in the Phase 6 report. Coverage, replacements, candidate ranking, certification gates, maps, tickets, payroll, attendance, production access, and deployment remain outside Phase 6.

## 2026-09-02 — Phase 8 static planning map

Implemented the bounded static planning map projection and privacy decisions. It is management-only, Published-schedule-only, and explicitly non-tracking; no production target, ticket integration, deployment, geocoding, GPS, routing, travel calculation, attendance, or automatic staffing was accessed or introduced.

## 2026-09-03 — Phase 8 completed

Completed the static planning map journey: protected current-Published Dubai-date projection; strict Team × operational scope; Super Admin exact and scoped-Admin coarse planning markers; browser-only OSM tiles, attribution, static association lines, drag/zoom/selection, and accessible tile-failure/list fallback. Migration, focused unit/component/PostgreSQL service, desktop/mobile Playwright, targeted lint, typecheck, build, seed smoke, and diff checks passed using fictional loopback data. No GPS, live tracking, geocoding, routes/travel, tickets, production access, or deployment was introduced.

## 2026-09-15 — Post-Phase-8 Checkpoint Sub-phase A

Remediated the aggregate verification harness and reconciled the status documentation after the Post-Phase-8 audit returned `CHECKPOINT_PASS_WITH_CAVEATS_READY_FOR_PHASE_9`. The shared component suite was repaired (a stale Phase 3 assertion and a Phase 6 import that crossed into `@/db/client`), aggregate integration now runs each integration file in its own freshly created loopback-only disposable database, one aggregate E2E command runs the Phase 1–8 guarded runners sequentially with runner-allocated ports, the historical root prototype was placed outside a documented lint boundary, and the roadmap/tracker/context/overview/index now record Phases 0–8 as completed with Phase 9 as the next `NOT_STARTED` journey. No business behavior, permission, privacy projection, workflow, schema, or Phase 8 map behavior changed; Phase 9 was not started, the Preview worktree was not touched, and no production system, secret, or `.env.production` value was accessed. Formal checkpoint closure is deferred to a separate Sub-phase B agent.

## 2026-09-15 — Post-Phase-8 Checkpoint Sub-phase B (closure)

Independently re-verified the Sub-phase A remediation commit `decb377decb32b3d064b14024c3079879dd932c0` from the committed `main` state and formally closed the Post-Phase-8 checkpoint. All twenty mandatory gates passed: typecheck, 42/42 unit, 20/20 database-free component, 8/8 isolated integration suites (41/41), the Phase 2–8 service runners, 8/8 migration, 8/8 aggregate E2E suites (60/60 with zero skips), 11/11 route certification, lint, isolated safe build, fictional seed smoke, isolation 9/9, and both whitespace checks. Phase 0–8 were confirmed validly implemented with no P0/P1 authorization, privacy, security, or data-integrity defect, and the maintained source contains no live-location, telemetry, or surveillance path. Disposable-database and loopback-port isolation were independently confirmed, the persistent configured test database was unchanged, and `.env.production` (size, mtime, and atime identical) and the Preview worktree were untouched. The checkpoint closed as `SCOPEIS_POST_PHASE_8_CHECKPOINT_COMPLETED_WITH_NON_BLOCKING_CAVEATS_READY_FOR_PHASE_9`; Phase 9 remains the next phase and was not started.

## 2026-09-15 — Phase 9 capability evidence journey completed

Delivered the bounded Phase 9 journey: employees maintain certifications, portfolio links and files, project examples, one active CV, and supporting capability documents from `/profile`; every submission saves immediately without an approval gate, flags its own new/updated state, and transactionally notifies active Super Admins; Super Admin reviews, verifies, removes verification, or resets from the employee detail context; owners update, replace files, and archive with preserved history. The additive `0010_phase_9_evidence_files.sql` migration extends the existing evidence/file tables (29 tables retained) with owner-submission, idempotency, verification-provenance, detail, uploader, and file-version fields plus partial unique indexes for one active CV per owner and review/file lookup indexes. Private bytes live behind one provider-neutral `put`/`read`/`remove` boundary resolved by `EVIDENCE_STORAGE_MODE` (local outside production, fail-closed `unconfigured` in production), keys are server-generated with no user filename, and no public URL exists. The locked privacy matrix gives the owner full access, Super Admin audited review access, scoped Admin certification summary facts only, and every other actor a non-enumerating refusal. Typecheck, 56/56 unit, 23/23 database-free component, 9/9 isolated integration suites, the Phase 2–9 focused service runners, 8/8 migration, 9/9 aggregate E2E suites (64/64 with zero skips), 12/12 route certification, lint, the isolated safe build, the fictional seed smoke with an evidence file, isolation 9/9, and both whitespace checks passed. Phase 7 coverage results are proven unchanged, sub-phase 9.9 remains deferred as an unapproved product decision, Phase 10 was not started, the Preview worktree and preserved untracked files were untouched, and no production system or `.env.production` value was accessed.
